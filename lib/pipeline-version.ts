// Pipeline version — bump this or set PIPELINE_VERSION env var on every deploy
// that changes extraction, assembly, or translation logic.
export const PIPELINE_VERSION = process.env.PIPELINE_VERSION || '2026.07.01-template'
